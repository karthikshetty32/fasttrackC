#define PI 3.14
#define RADIUS(X) X*X


char upper_to_lower(char X); //1.Write a program that converts upper case to lower.

float area_of_circle(float radius); //2.Write a program to calculate the area of circle.

float simple_interest(float principle, float time, float rate); //3.Write a program to calculate simple and compound interest.

float compound_interest(float principle, float time, float rate); //3.

float celsius_to_fahrenheit(float celsius); //4.Write a program to convert temperature from Celsius to Fahrenheit and Fahrenheit to Celsius

float fahrenheit_to_celsius(float fahrenheit); //4.

int is_even_or_odd(int number); //5.Check if entered number is even or odd.

int is_leap_year_or_not(int year); //6.Check if entered year is leap year or not.

int left_shift_operator(int N); //7.WAP to find power of 2^N using left shift operator.
